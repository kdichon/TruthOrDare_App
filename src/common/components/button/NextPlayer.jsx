import {View, Text} from 'react-native';
import React from 'react';
import {Button} from 'react-native-paper';

const NextPlayer = () => {
  return (
    <Button icon="arrow-right" mode="outlined">
      Next
    </Button>
  );
};

export default NextPlayer;
